document.addEventListener("DOMContentLoaded", function() {
    let goalPercentage = 40; // This would be dynamic based on data
    document.getElementById('goal-percentage').textContent = goalPercentage + '%';
});
